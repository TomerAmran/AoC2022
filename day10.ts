function part1(input: string[]) {
    let register = 1
    let cycle = 0
    input.forEach(_ => {
        if (_ === 'noop') {
            cycle++
        }
        else {
            const [_addx, val] = _.split(' ')
            
        }
    })
}